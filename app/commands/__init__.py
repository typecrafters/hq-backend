"""Management commands.

Each module in this directory should define a `register(registry)` function
that adds commands to the registry.

Example:

    def register(registry):
        registry.add(
            name="my_command",
            help="Description of what it does",
            fn=my_command_function,
        )

    def my_command_function(args):
        # your logic here
        pass
"""
